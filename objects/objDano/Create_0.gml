/// @description Insert description here
var cl = camera_get_view_width(view_camera[0]);
var ca = camera_get_view_height(view_camera[0]);

escalaX = display_get_gui_width()/cl;
escalaY = display_get_gui_height()/ca;

alvo = -1;

xx = alvo.x;
yy = alvo.y;

dano = -1;
alpha = 1;
