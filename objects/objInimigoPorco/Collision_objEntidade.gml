/// @description Insert description here
// You can write your code in this editor
if (global.pause) exit;
var _inimigo = instance_place(x, y + 1, objEntidade)

if tomarDano && mask_index = sprPlayerIdle && !isFall && !_inimigo.dead {
		var danoPig = objInimigoPorco.dano;
		lifes -= danoPig;
		alarm[1] = 10;
		alarm[0] = 120;
		tomarDano = false;
		
		if direc = 0 || direc = 2 {
			var dir = point_direction(x, y, objPlayer.x - 10, objPlayer.y);
			empurrarDir = dir;
		}
		
		if direc = 1 || direc = 3 {
			var dir = point_direction(x, y, objPlayer.x + 10, objPlayer.y);
			empurrarDir = dir;
		}
}




